using UnityEngine;
//C 2025 Daniel Snapir alias Baltazar Benoni

public class OldInputStuff
{
    public class NumberInputOld
    {
        /*public override void NewText()
        {
            int hours = sum >= 10000 ? sum / 10000 : 0;
            int minutes = sum >= 100 ? (sum - hours * 10000) / 100 : 0;
            int seconds = sum - minutes * 100 - hours * 10000;
            string addition1 = GetZerosToAdd(hours);
            string addition2 = GetZerosToAdd(minutes);
            string addition3 = GetZerosToAdd(seconds);
            string txt = $"{addition1}{hours}:{addition2}{minutes}:{addition3}{seconds}";
            this.text = txt;
        }*/
    }
    
}
